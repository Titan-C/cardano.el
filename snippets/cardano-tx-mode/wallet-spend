# -*- mode: snippet -*-
# key: wtx
# name: wallet-spend
# expand-env: ((yas-indent-line 'fixed))
# --

# Wallet: `(cardano-wallet-name cardano-wallet-current)`
# ID: `(cardano-wallet-id cardano-wallet-current)`
# Lovelace balance: `(cardano-wallet-total cardano-wallet-current)`

outputs:
  - address:  $1
    amount:
      lovelace:  $0
